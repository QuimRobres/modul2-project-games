<div class="game-page">
    <img src='{{this.image_url}}' alt='game-image'>

    <div>
        <form class="one" action='/private/gameDetail/own' method="POST">
            <input type="text" name="id" value={{this._id}} style="display: none;">
            <button class="own" type='submit' >Add game to own list</button>
            <button class="wish" type='submit' formaction='/private/gameDetail/wish' >Add game to wishlit</button>
    </div>

    

    <div>
        <h5>{{this.name}}</h5>
        <p>{{this.description_review}}</p>
        <p>Players: {{this.min_players}} - {{this.max_players}}</p>
        <p>Playtime: {{this.min_playtime}} - {{this.max_playtime}} mins</p>
        <p>Rating: {{this.average_user_rating}}</p>
        <p>{{this.price}}</p>
    </div>
    <div>
        <p>{{this.year.published}}</p>
        <p>{{this.min_age}}</p>
    </div>
</div>






{{!-- Name Price
nº Players Genre
Time rating
Age Description
Publisher official page link --}}